pub bulce_loop () {         loop {        println!("Escribe algo (o 'exit' para salir):");        let mut entrada = String::new();        io::stdin().read_line(&mut entrada).expect("Error al leer la línea");        let entrada = entrada.trim();        if entrada == "exit" {            println!("Saliendo...");            break        } else {            println!("Escribiste: {}", entrada);        }    }}